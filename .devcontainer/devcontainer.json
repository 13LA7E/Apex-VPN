{
  "name": "APEX VPN - Instant Summit ⚡",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postStartCommand": "nohup bash /workspaces/Apex-VPN/auto-start.sh > /tmp/codespace-startup.log 2>&1 &",
  "postCreateCommand": "chmod +x /workspaces/Apex-VPN/apex-vpn && chmod +x /workspaces/Apex-VPN/auto-start.sh",
  "privileged": true,
  "runArgs": ["--cap-add=NET_ADMIN", "--device=/dev/net/tun"],
  "remoteUser": "vscode"
}